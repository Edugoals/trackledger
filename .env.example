DB_USER=trackledger
DB_PASSWORD=trackledger
DB_NAME=trackledger
DB_ROOT_PASSWORD=rootpassword
NODE_ENV=development

# Google OAuth (zie README)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Gebruik 5173 zodat de callback via de Vite-proxy gaat (session cookie werkt dan)
GOOGLE_REDIRECT_URI=http://localhost:5173/api/auth/google/callback

# URLs
CLIENT_URL=http://localhost:5173
SESSION_SECRET=verander-deze-naar-een-random-string
